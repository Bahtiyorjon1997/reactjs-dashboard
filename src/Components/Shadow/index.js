import React, { useState } from "react";
import styled from "styled-components";

const Shadow = () => {
  <ShadowWrapper>halo</ShadowWrapper>;
};

export default Shadow;

const ShadowWrapper = styled.div`
  width: 10%;
  height: 50px;
`;
